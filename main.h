#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdarg.h>
#include <string.h>

int add(int count, ...);
int subtract(int count, ...);
int operation(void);
int division(int count, ...);
int multiply(int count, ...);
void welcome(void);


#endif